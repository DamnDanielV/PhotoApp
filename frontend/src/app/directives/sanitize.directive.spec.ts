import { SanitizeDirective } from './sanitize.directive';

describe('SanitizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SanitizeDirective();
    expect(directive).toBeTruthy();
  });
});
